build a python application to replaces the dates in igazolas.jpg
the input is date, which should be generated as handwritten and replaced in position 1347x750 to 1966x855 and 2386x759 to 2934x855.
The current date should be generated as handwritten and replaced in position 921x1403 to 1685x1495.
Save the file as a new jpg, filename is igazolas_{current month numeric}_{current day numeric} eg igazolas_12_05.jpg.
Select the proper image library to do the replacement.
Remove the current dates by filling the image with the same background.
Try to match the ink color of the newly added text.
The year should be just 25 instead of 2025